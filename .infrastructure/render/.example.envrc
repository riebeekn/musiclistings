# This is a template file containing the environment settings
# required to run Terraofm.  Copy this file to .envrc
# and update the setting values with the appropriate values from 1Password

# You will need to run source .envrc from your terminal...
# Or install https://direnv.net/docs/installation.html
export AWS_ACCESS_KEY_ID=<access key id>
export AWS_SECRET_ACCESS_KEY=<secret access key>
